{
    "builds": [{
        "src": "restaurant_project/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "restaurant_project/wsgi.py"
        }
    ]
}